export { invoke } from '@tauri-apps/api/core';

// Typed IPC bindings land here once specta is wired up on the Rust side.
// Until then, features that need to call Rust use `invoke` directly with a
// string command name.
